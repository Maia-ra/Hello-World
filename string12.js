isNightTime ? console.log('Turn on the lights!') : console.log('Turn off the lights!');

// Simplificando bloco de if e else
// É verificado se a primeira condição é verdadeira, sendo ela é executada
// A primeira sendo falsa, o segundo é executado